# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: checkout.spec.js >> Checkout page >> visiting /checkout without session shows redirect message
- Location: tests\e2e\checkout.spec.js:4:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText(/no checkout session found/i)
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByText(/no checkout session found/i)

```

```yaml
- heading "404" [level=1]
- heading "Page Not Found" [level=2]
- paragraph: The page you are looking for might have been removed, had its name changed, or is temporarily unavailable.
- link "Go Back Home":
  - /url: /
- alert
```

# Test source

```ts
  1  | const { test, expect } = require('@playwright/test');
  2  | 
  3  | test.describe('Checkout page', () => {
  4  |   test('visiting /checkout without session shows redirect message', async ({ page }) => {
  5  |     await page.goto('/checkout');
> 6  |     await expect(page.getByText(/no checkout session found/i)).toBeVisible();
     |                                                                ^ Error: expect(locator).toBeVisible() failed
  7  |     await expect(page.getByText(/redirecting/i)).toBeVisible();
  8  |   });
  9  | 
  10 |   test('redirects away from /checkout after a few seconds', async ({ page }) => {
  11 |     await page.goto('/checkout');
  12 |     // Should redirect to /tours within ~4 seconds
  13 |     await page.waitForURL(/\/tours/, { timeout: 5000 });
  14 |     await expect(page).toHaveURL(/\/tours/);
  15 |   });
  16 | });
  17 | 
```